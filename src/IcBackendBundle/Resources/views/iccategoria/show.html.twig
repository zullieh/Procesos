{% extends 'base_backend.html.twig' %}

{% block body %}
    <div class="col-md-6 col-md-offset-1" style="margin-top: 5%">
        <div class="row form-group">
            <label for="">Nombre</label>
            <div class="input-group">
                <input type="text" value="{{ icCategoria.nombre }}" disabled class="form-control">
                <span class="input-group-addon success"><i class="fa fa-check" aria-hidden="true"></i></span></span>
            </div>
        </div>
    </div>
    <div class="col-md-5" style="margin-top: 5%">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <h3 class="panel-title" align="center">
                    <i class="fa fa-star" aria-hidden="true"></i> Acciones</h3>
            </div>
            <div class="panel-body">
                <div class="" align="center">
                    {#<a href="{{ path('icarea_new') }}"><button type="button"  class="btn btn-sm btn-success">Nuevo</button></a>#}
                    <a href="{{ path('iccategoria_index') }}"><button type="button" class="btn btn-sm btn-default"> Regresar</button></a>
                    <a href="{{ path('iccategoria_edit', { 'idCategoria': icCategoria.idCategoria }) }}"><button type="button" class="btn btn-sm btn-warning btn-create">Editar</button></a>
                    <input class="btn btn-sm btn-danger" type="submit" value="Borrar">
                </div>
            </div>
        </div>
    </div>
    {{ form_start(delete_form) }}
    {{ form_end(delete_form) }}


{% endblock %}
