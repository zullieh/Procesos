{% extends 'base_backend.html.twig' %}

{% block content %}
<div class="container">
    <div class="row col-md-6 col-md-offset-2 custyle">
        <table class="table table-striped custab">
            <tbody>
            <tr>
                <th>Idsubcategoria</th>
                <td>{{ icSubcategoria.idSubcategoria }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ icSubcategoria.nombre }}</td>
            </tr>
        </tbody>
    </table>
        <div class="col col-xs-6 text-right">
            <a href="{{ path('icsubcategoria_new') }}"><button type="button"  class="btn btn-sm btn-primary btn-create">Nuevo</button></a>
            <a href="{{ path('icsubcategoria_index') }}"><button type="button" class="btn btn-sm btn-primary btn-create"> Regresar</button></a>
            <a href="{{ path('icsubcategoria_edit', { 'idSubcategoria': icSubcategoria.idSubcategoria }) }}"><button type="button" class="btn btn-sm btn-primary btn-create">Editar</button></a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input class="btn btn-sm btn-primary" type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
